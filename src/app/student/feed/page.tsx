import FeedClient from "./FeedClient";

export const metadata = {
  title: "Student Feed",
  description: "Student feed placeholder after completing profile setup.",
};

export default function StudentFeedPage() {
  return <FeedClient />;
}
